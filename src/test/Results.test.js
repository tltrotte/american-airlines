import React from "react";
import Results from "../Results.js";
import { expect } from "chai";

describe("Form", () => {
	let component;
	it("shows results cards", () => {
		expect(component.find(""));
	});
});
