import React from "react";
import App from "../App.js";
import { expect } from "chai";

describe("App", () => {
	let component;
	it("shows logo image", () => {
		expect(component.find(""));
	});
	it("show Results component", () => {
		expect(component.find(""));
	});
	it("Form component", () => {
		expect(component.find(""));
	});
});
